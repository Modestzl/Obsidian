>[! tip]- Очистка оперативной памяти
>```cach
>sync; echo 1 > /proc/sys/vm/drop_caches
>или более безопасная
>sync && echo 3 > /proc/sys/vm/drop_caches
> - `3` = очистка page cache + dentries + inodes (более полная очистка)
> - - `&&` вместо `;` — прекратит выполнение, если sync не удастся
 >```


>[!tip]- Очищаем кэш. autourl.
>```
> 	1958  cd /usr/app/amital.ru/system/cache
 > 	    1959  du -h ./autourl/
 > 		 1960  find ./autourl/ -type f -name 'cache*.*' -print0 | xargs -0 rm
 >  // Или более удобной и безопасный.
 >    find ./autourl/ -type f -name 'cache*' -delete
>```


>[!tip]- индексация поисковика
>```
> 1933  /usr/scripts/indexer.sh
 1934  searchd
>```

