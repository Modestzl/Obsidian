---
tags: видео/vk
source: "[[Ссылка на оригинал]]"
date: 2026-07-22
---
[[DEV]][[Рабочие моменты]] [[test cmds]]

>[!tip]- Linux ```start```
>```
>	- sudo apt install tmux
>	- sudo apt install xclip
>	- 
>// # показать только тех, у кого shell присутствует в /etc/shells (список разрешённых оболочек)
>comm -12 <(grep -v '^#' /etc/shells | sort) <(awk -F: '{print $NF}' /etc/passwd | sort) | while read sh; do grep ":$sh$" /etc/passwd; done
># Просто сокращенный Вариант. Но может подхватить лишнего в вывод.
>grep -vE '/nologin$|/false$' /etc/passwd
># Где файлы программы
>whereis vim  или which vim или vim --version 
>```



# Безопасное редактирование системных файлов
sudo visudo  # для редактирования /etc/sudoers

# С вашим любимым редактором
sudo nano /etc/hosts
sudo vim /etc/fstab
fc 121 122 - открывает текстовый редактор с текстом команд, потом сохранить выполняются автоматом при выходе

>[!tip]- Сравнить два файла ```comm```
>```
>	// **Важное условие:** оба файла должны быть **предварительно отсортированы**. Если нет — сначала сортируем:
>	comm -12 <(sort 1.txt) <(sort 2.txt)
>	
>// # показать только тех, у кого shell присутствует в /etc/shells (список разрешённых оболочек)
>comm -12 <(grep -v '^#' /etc/shells | sort) <(awk -F: '{print $NF}' /etc/passwd | sort) | while read sh; do grep ":$sh$" /etc/passwd; done
># Просто сокращенный Вариант. Но может подхватить лишнего в вывод.
>grep -vE '/nologin$|/false$' /etc/passwd
>```

>[!tip]- Красиво скопировать файл добавляя символы 
>```
>cp test01{,.ttt}
>cp test01 test01.ttt
>```


$ cat fruits.txt | sort > sorted_fruit.txt

date | xclip -selection clipboard # Работает Ctrl+V

date | xclip -sel clip

**

!151 — выполнить команду под номером 151 из истории;

!151: — показать команду номер 151, но не выполнять её;

!! — повторно выполнить последнюю введенную команду.

**


# {{Настройка bash}}

## 🎯 Основные тезисы
- Тезис 1...
- Тезис 2...

## 📝 Подробный конспект
Содержание...

## 🧠 Термины и идеи
- **Сложный термин** — объяснение.

## ✍️ Мои заметки по ходу просмотра
(Сюда пишем руками во время просмотра через Media Notes)

## 💎 Цитаты
> Точная цитата 1 (см. [[субтитры.srt]], момент ~05:23)

## ✅ Action Items
- [ ] Внедрить идею Х в проект Y