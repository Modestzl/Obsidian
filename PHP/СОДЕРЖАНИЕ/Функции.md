[[Видео уроки Кудлай.]]

Функция  PHP может принимать неограниченное количество аргументов.  Или список аргументов переменной длины.

```
<php error_reporting(-1);
	function sum(...$nums)  
{  
    $result = 0;  
    foreach ($nums as $num) {  
        $result += $num;  
    }  
    return $result;  
}  
  
echo 'Результат суммирования '. sum(1,2,3,4,5);

```



> [!example]- Пример кода c  конструкцией function ```sum($a, $b...$nums);```
>
> ```<?php error_reporting(-1);
>function sum($a, $b,...$nums)  
>{  
 >   $result = $a*$b;  
>    foreach ($nums as $num) {  
 >       $result += $num;  
 >   }  
 >   return $result;  
> } 
> echo 'Результат суммирования '. sum(1,2,3,4,5);
> // Здесь мы сперва умножаем, $a $b к результату потом плюсуем все остальные Аргументы.
> ```

>[!important]- Указание типов переменных.
>```
>// Указываем тип при объявлении переменной.
>	int $a , int $b
>// Или функции указать тип возвращаемых значений.
>	function sum($a, $b):int ( существует значение void- ничего не возвращает)
>// можем проверить тип Возвращаемого значения.
>	var_dump (sum(1,5,3)); 
>		# Обратите внимание на аргументы в скобочках. Это называется позиционные аргументы. В 8 версии появились [[именованные аргументы.png]]..
>```
>[Кудлай. 25. Пользовательские функции. Часть-2](https://vkvideo.ru/video-235378893_456239064?pl=-235378893_2&t=7m25s)

 - Если функция ничего не возвращает, то она по умолчанию будет возвращать NULL.
 - После команды return в теле функции. Происходит прерывание. Всё, что будет дальше не исполняется.


 ![[обнуляемый тип.png]]

![[именованные аргументы.png]]


